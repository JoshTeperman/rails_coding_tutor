<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Tutors</title>
  <%= render "shared/navbar" %>
</head>
<body>
  <div style="margin: 50px;"><h1>All Tutors</h1></div>

  <div class="d-flex flex-wrap">
  <% @profiles.each do |profile| %>
    <% if profile.tutor?  && profile.id != 86 && profile.id != 85 %>   <%# profile.id 85 & 86 are the admin and moderator users %>
      <div class="card m-2" style="width: 21.5rem;">
        <div class="card-body text-center">
            <% if profile.avatar.attached? %>
                <%= image_tag profile.avatar.service_url, class: 'image-size' %>
            <% else %>
                <div style="height: 100px;"><%= image_tag "https://avatarfiles.alphacoders.com/182/182752.jpg", class: 'image'%></div>
            <% end %>
          <h2 class="card-title"><%= profile.first_name %> <%= profile.surname %></h2>
          <h6 class="card-subtitle mb-2 text-muted"><%= profile.skills %></h6>
      
          <%= link_to ('<i class="fa fas fa-user-circle fa-2x"></i>').html_safe, profile_path(profile) %> |
          <%= link_to ('<i class="fa fas fa-book fa-2x"></i>').html_safe, new_booking_path(:tutor => profile.tutor_id) %>
        </div>

      </div>
    <% end %>
  <% end %>
</div>

</body>
</html>





